{
  "project": "clinical_connectome",
  "input_path": "data/derived/lesion_matrix/25-08_s1.2",
  "reduction_method": "tsne",
  "params_file": "config/registry/params_reduction.json",
  "output_root": "results/lesion/dim_reduction",
  "session_name": "s1.2",
  "overwrite": false,
  "fine_tuning": true,
  "color_by": ["dataset", "side", "volume", "nihss"],
  "viz_n_components": 2,
  "write_embeddings_grid": true,
  "save_tuning_embeddings": true,
  "precompute_distance_metric": true,
  "run_notes": "tsne tuning on s1.2 matrix (5269 subjects, +UCL-UK/UCLStrokeData) - same tuning_grid as the last s1.1 sweep (metric x perplexity, nested on metric). First real measurement of whether the euclidean precompute fix (extended to tsne 25-08 without independent timing, see docs/debugging/debug_25_08_26.md) actually helps for t-SNE, not just UMAP."
}