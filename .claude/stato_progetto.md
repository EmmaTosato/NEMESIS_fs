# Stato progetto — NEMESIS

Ultimo aggiornamento: 2026-09-02. **Snapshot dello stato attuale — non un log cronologico.** Riscritto in place a ogni sessione (nessuna sezione per-sessione, nessun accumulo): descrive solo ciò che è vero *oggi*. La narrativa di cosa è successo sessione per sessione **non vive più qui** — è coperta da git log/commit message, `docs/debugging/` (narrativa completa per sessione di debug), `.claude/lessons_learned.md` (pattern di errore generalizzabili, deduplicati), `runs.csv`/`runs_tuning.csv` + `logs/`/`summaries/` (provenienza di ogni esecuzione pipeline). Le sessioni registrate prima della conversione a snapshot (10/08) restano, congelate, in `.claude/stato_progetto_archive.md` (sola lettura, non più aggiornato).

## Architettura / cosa è implementato

Sintesi corrente in `README.md` ("What's implemented so far") — non ripetuta qui. Riferimento architetturale sviluppatore: `docs/dev/` (`retrieval.md`, `models.md`, `config.md`, `plotting.md`, `lesion_matrix.md`, `fc_matrix.md`, `sdc_matrix.md`, `design_patterns.md`, `metadata.md` — nuovo 02-09-26, i 3 livelli di metadati e come si relazionano).

`src/analysis/plotting.py::_CATEGORICAL_PALETTE` estesa 5→10 colori (01-09-26, commit `4b4111e`), CVD-safe solo su coppie adiacenti (non all-pairs — limite strutturale per uno scatter con >3-4 serie, non risolvibile ampliando la palette, vedi `docs/dev/plotting.md`). Stesso commit: `_MARKER_SIZE`/`_MARKER_ALPHA`/`_declutter_points` (già validati sui plot di embedding) ora applicati anche a `plot_clusters_2d`/`plot_clusters_comparison`/`plot_silhouette_analysis`.

`src/pipeline/embedding_app.py` (Dash) esteso 01-09-26 (commit `ea66600`, merge `714d2b5`) con due pannelli di anatomia interattivi — click su un punto → viewer 3D `nilearn` della lesione reale del paziente; per un run `clustering.py`, dropdown cluster → overlap/frequency map lesionale del cluster — più un selettore a 7 passi (step "Parametri" per gli iperparametri propri del metodo di clustering). Nuovo modulo `src/analysis/anatomical_maps.py` (risoluzione path lesione + overlap map, caricamento parallelo su thread pool). Guida completa: `docs/guides/embedding_app.md`. Questa è la versione attiva su `main` — vedi sotto per un renderer alternativo in valutazione su branch separato.

`src/pipeline/enrich_lesion_metadata.py` ridisegnato 02-09-26 (branch `metadata-restructuring`, non ancora mergeato): `write_in_place`/`copy_output_path` sostituiscono il vecchio `output_root`-driven auto-naming; `data/derived/clinical_metadata/` eliminata. Vedi `docs/dev/metadata.md`.

## Lavoro attivo / thread aperti

- **Naming convention sessioni dati (sX.Y[-formato]) — decisa, non ancora applicata**: convenzione concordata con l'utente: `sX` = track/modalità, `sX.Y` = coorte, `-<formato>` = suffisso obbligatorio dove un track ammette più di un formato grezzo (oggi solo SDC/FC/lesion futuro parcellato — `vol` per volumetrico/nifti, nome atlante hyphen-slugified per parcellato, es. `schaefer-200-tian-s2`; mai `_` nel tag, vincolo di `src/utils/run_log.py::append_run_log_entry` che splitta `run_id` sul primo `_`). Retroattivo anche sui nomi già esistenti (deciso esplicitamente dall'utente, "anche se dobbiamo toccare ogni file"). **Applicato finora solo al `session_name` delle 3 run di refresh `enrich_lesion_metadata.py` di oggi** (`s1.1-vol`, `s1.2-vol`, `s2.1-schaefer-200-tian-s2`, solo nel `runs.csv` di quella pipeline) — **non ancora** a: le directory fisiche su disco (`data/derived/lesion_matrix/21-07_s1.1/` resta col nome vecchio), i config `session_name` delle altre pipeline (`build_lesion_matrix.json`/`build_sdc_matrix.json`/`dim_reduction.json`/`clustering.json`), i ~100+ file `runs.csv`/`runs_tuning.csv` e le cartelle risultato sotto `results/lesion/`, `results/sdc/`, il track FC (`s2`→`s3.1`, mai stato applicato, `fc_matrix`/`masked_fc` session_name ancora "s2"), `docs/experiments/data_sessions.md` (il registro stesso). Scope completo inventariato in sessione ma non ancora eseguito — vedi "Prossimo passo esatto".
- **Parser `scripts/build_dim_reduction_strategies_csv.py`**: cerca header `## Session` (H2) in `docs/experiments/data_sessions.md`, che invece usa H1/H3 — il join non ha mai fatto match, fallback silenzioso a "undocumented" (mai risolto, indipendente dal task di rename ma va sistemato insieme).
- **Renderer alternativo per i pannelli di anatomia (niivue)**: esplorazione su branch `viz-niivue` (4 commit, non mergeato — `main` resta su nilearn, su richiesta esplicita). Nessuna decisione presa su quale renderer tenere — l'utente ha pianificato di provare più alternative, un branch per volta. Per riprendere: `git checkout viz-niivue`, `python -m src.pipeline.embedding_app`.
- **Lesion clustering produzione s1.1**: 6 opzioni di k lanciate e documentate (`docs/experiments/clustering/s1_production.md`). Nessuna scelta finale ancora fatta.
- **SDC clustering tuning s2.1**: nc2 documentato (`docs/experiments/clustering/s2_tuning.md`), lateralizzazione su `lesion_side` confermata. Prossimo step già scritto nel doc: estendere a `nc3`.
- **Audit naming plot-level**: ancora aperto, non toccato di recente.

## Vincoli/regole in vigore oggi

- **Branch**: lavoro corrente su `metadata-restructuring` (1 commit avanti su `main`, non mergeato/pushato). `main` resta la base stabile.
- **`config/registry/params_clustering.json`**: lasciato ai valori dell'ultima opzione lanciata per metodo (kmeans/agglomerative/gmm su opzione C: k6; hdbscan su opzione F: mcs15/ms5; spectral su opzione D: k8/rbf/gamma1.0) — non sono i default "canonici" del repo, sono lo stato dell'ultima run reale.
- **`clustering.py` non genera più `comparison/`** (rimosso 01-09-26): cartelle `comparison/` residue da run precedenti restano su disco ma non vengono più rigenerate.
- **Esecuzione locale vs SLURM**: locale diretto è il default, non proporre `sbatch`/jobs cluster senza richiesta esplicita.
- **`src/sdc/`/`src/pipeline/compute_sdc.py`** non eseguibili/testabili in locale (richiedono `bcblib`) — `pytest --ignore` su `tests/unit/test_sdc_staging_and_check.py`/`tests/integration/test_compute_sdc_pipeline.py` fuori dal cluster.
- **Documentazione e Codice**: niente dump di tabelle o dati completi nei `.md` (solo pointer a `results/*.csv`). Rationale/logica verbosa confinata in `docs/dev/`, il sorgente python deve rimanere conciso e snello.
- **Diagnostic grid oltre 2D**: `n_components > 2` non emette un diagnostic grid automatico — ispezione solo on-demand via `scripts/plot_tuning_embedding_3d.py`, non è un bug.
- **Copie locali ridotte**: solo `UNIPD/WashU/features/` e `data/derived/features/masked_fc/` restano a 10 soggetti campione (vedi `docs/guides/datasets.md`) — `manual_masks` è pieno per tutti e 5 i dataset dal 01-09-26.
- **`assets/metadata/*.tsv`**: verificati e puliti al 100% (Check A/B/C, 02-09-26) — rigenerati da `notebooks/exploration/dataset_exploration.ipynb` (ora con `dtype=str`), nessun drift residuo. `_participants_features.tsv`/`_participants_join.tsv` (solo WashU) restano senza consumer — non morti, in attesa di un meccanismo di arricchimento FC-specifico non ancora scritto.

## Prossimo passo esatto

Eseguire il task di re-naming delle sessioni dati (vedi thread aperto sopra) — con la convenzione già decisa (`sX.Y[-formato]`) ma applicazione ancora da fare: script di dry-run (mostra i rename senza scrivere, dato che `data/derived/`/`results/` sono gitignored e non hanno rete di sicurezza git) su tutte le directory/CSV coinvolte, poi rename vero solo dopo conferma esplicita dell'utente sull'output del dry-run. Include: rinominare le directory fisiche, aggiornare i `session_name` nei config `build_*.json`/`dim_reduction.json`/`clustering.json`, riscrivere le colonne `session`/`input_path`/`output` in ogni `runs.csv`/`runs_tuning.csv` interessato, correggere il track FC da `s2` a `s3.1`, aggiornare `docs/experiments/data_sessions.md` (il registro) e `data_employing.md`, e — colto durante lo stesso lavoro — sistemare la regex H2/H3 di `scripts/build_dim_reduction_strategies_csv.py`.

In alternativa (se si rimanda il rename): decidere il k di produzione finale per lesion s1.1, estendere l'analisi SDC s2.1 a `nc3`, riprendere l'audit naming a livello di plot, o continuare a provare renderer alternativi per i pannelli di anatomia (branch `viz-niivue`).
